/**
 * Re-export types from @pyreon/document.
 * These are the types that connector-document works with.
 */
export type {
  DocChild,
  DocNode,
  NodeType,
  ResolvedStyles,
} from "@pyreon/document"
