# Changesets

Hello and welcome! This folder has been automatically generated by `@changesets/cli`, a build tool that works
with multi-package repos, or single-package repos to help you version and publish your code. You can
find the full documentation for it [in the readme](https://github.com/changesets/changesets/blob/main/packages/changesets-cli/README.md)

We use changesets for versioning and publishing. To add a changeset:

```bash
bun changeset
```

This will prompt you to select packages and describe the changes.
